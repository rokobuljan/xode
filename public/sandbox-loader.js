// This loader is used when embedding the sandbox iframe.
// It exists to make the sandbox page easier to deploy and keep the editor page isolated.

window.addEventListener('DOMContentLoaded', () => {
    // no-op, just to support future loader logic if needed.
});
